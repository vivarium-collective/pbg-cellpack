"""Custom bigraph-schema types for cellPACK wrapper.

Currently uses only built-in types.  This module is a hook for future
registration of custom types (e.g. mesh-based compartment geometries,
unit-bearing radii).
"""


def register_cellpack_types(core):
    """Register custom types with a bigraph-schema core."""
    pass
